BEGIN;
CREATE TABLE IF NOT EXISTS favorites(
   user_id  INTEGER UNIQUE NOT NULL,
   age INTEGER DEFAULT 0 NOT NULL,
   sex INTEGER DEFAULT 0 NOT NULL,
   animal INTEGER DEFAULT 0 NOT NULL,
   music INTEGER DEFAULT 0 NOT NULL,
   sport INTEGER DEFAULT 0 NOT NULL,
   movie INTEGER DEFAULT 0 NOT NULL,
   book INTEGER DEFAULT 0 NOT NULL
);
COMMIT;
